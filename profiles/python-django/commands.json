{
  "profile": "python-django",
  "filePatterns": ["**/*.py", "manage.py", "*/settings*.py"],
  "commands": {
    "test": "pytest --cov --cov-report=term",
    "lint": "ruff check .",
    "format": "black .",
    "typecheck": "mypy .",
    "build": "python manage.py collectstatic --noinput && python manage.py check --deploy",
    "audit": "pip audit",
    "e2e": "playwright test",
    "coverage": "pytest --cov --cov-report=term-missing",
    "format-check": "black --check .",
    "logs": "tail -f logs/*.log 2>/dev/null || journalctl --user -f"
  }
}
