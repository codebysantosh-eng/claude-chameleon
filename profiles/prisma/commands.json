{
  "profile": "prisma",
  "filePatterns": ["prisma/**/*.prisma", "**/*.prisma", "prisma/migrations/**"],
  "commands": {
    "test": null,
    "lint": "npx prisma validate",
    "format": "npx prisma format",
    "typecheck": null,
    "build": "npx prisma generate",
    "audit": "npm audit --production",
    "e2e": null,
    "coverage": "npx prisma validate",
    "migrate": "npx prisma migrate dev",
    "deploy": "npx prisma migrate deploy",
    "format-check": "npx prisma format --check",
    "logs": "npx prisma studio"
  }
}
